clc;
clear all;
close all;
dirIn = 'Enter directory having marked data folders';
di = dir(dirIn);
di = di(3:length(di));

paths = {};
for i = 1:length(di)
    paths{i} = strcat(dirIn,'\',di(i).name);
end

for i = 1:length(paths)
    dataprep1(paths{i},paths{i});
    features(paths{i},paths{i});
    train_target(paths{i},paths{i});   
end


train_test_god(paths,di);